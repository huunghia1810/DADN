//---------import constant---------

import * as constantGeneral from "../constants/General"

class ActionGeneral {
    /*setIsProcessing(isProcessing) {
        return function (dispatch) {
            dispatch({type: constantGeneral.GENERAL_SET_STATUS_PROCESSING, isProcessing: isProcessing});
        }
    }*/
}

export default new ActionGeneral();